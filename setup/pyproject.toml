[project]
name = "setup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.1"
dependencies = [
    "pandas>=3.0.5",
]

[dependency-groups]
dev = [
    "nbstripout>=0.9.1",
]
